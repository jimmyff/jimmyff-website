title = "jimmyff"
description = "Coder, Maker, Gamer (he/him) 🏳️‍🌈"

# The URL the site will be built for
base_url = "https://www.jimmyff.co.uk"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = true
minify_html = true
default_language = "en"

theme = "zola-inky"

# The taxonomies to be rendered for the site and their configuration of the default languages
# Example:
#     taxonomies = [
#       {name = "tags", feed = true}, # each tag will have its own feed
#       {name = "tags"}, # you can have taxonomies with the same name in multiple languages
#       {name = "categories", paginate_by = 5},  # 5 items per page for a term
#       {name = "authors"}, # Basic definition: no feed or pagination
#     ]
#
taxonomies = [
    { name = "tags", lang = "en", rss = true },
    { name = "medium", lang = "en", rss = true },
    { name = "series", lang = "en", rss = true },
    { name = "projects", lang = "en", rss = true },
]

[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true


[extra]

# Put all your custom variables here
google_analytics_tag_id = 'G-71ZN1D4H6Q'

keywords = "jimmy, forrester-fellowes, jimmyff, jimmy3ds, yorkshire, leeds, developer"
avatar = "/avatar_2024.webp"


image_resizing_disabled = false
image_format = "webp"             # "auto", "jpg", "png", "webp"
image_quality = 80
thumbnail_quality = 70
search_path = "$BASE_URL/search/"

# responsive image shortcode 
# example: {{ image(src="foo.jpeg", alt="some image") }}
images_default_size = 1024
images_sizes = [512, 1024, 2048]

# link_twitter_username = "jimmyff_uk"
# link_mastodon_url = "https://fosstodon.org/@jimmyff"
link_bluesky_url = "https://bsky.app/profile/faehound.games"
link_github_username = "jimmyff"
link_medium_username = "jimmyff"
link_instagram_username = "jimmyff_art"

# I have added credit to the zola & the theme on my main homepage under the heading "About this site"
disable_powered_by = false

menu_links = [
    { url = "$BASE_URL/", name = "Home", emoji = "🏠" },
    { url = "$BASE_URL/blog/", name = "Blog", emoji = "📖" },
    { url = "$BASE_URL/gallery/", name = "Gallery", emoji = "🎨" },
    { url = "$BASE_URL/apps/", name = "Apps", emoji = "📱" },
    # { url = "$BASE_URL/apps/", name = "Projects", emoji = "💡" },
    { url = "$BASE_URL/boardgames/", name = "Games", emoji = "🎲" },
    # { url = "$BASE_URL/guestbook/", name = "Guestbook", emoji = "🌍" },
    # { url = "$BASE_URL/search/", name = "Search", emoji = "🔎" },

]
